import ComingSoon from "@/components/ComingSoon";

export const metadata = { title: "Sign in — primer." };

export default function LoginPage() {
  return (
    <ComingSoon title="Sign in">
      Register and sign in are wired on the backend already &mdash; cookie sessions, the
      lot. The screens for them are the next thing to build.
    </ComingSoon>
  );
}
