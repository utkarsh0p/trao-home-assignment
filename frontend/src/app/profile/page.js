import ComingSoon from "@/components/ComingSoon";

export const metadata = { title: "Profile — primer." };

export default function ProfilePage() {
  return (
    <ComingSoon title="Profile">
      Your account and a way to sign out. Arriving with the auth screens.
    </ComingSoon>
  );
}
